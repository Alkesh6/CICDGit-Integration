package basicTest.BasicUIOps;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

public class traditionalDropdown {
	public static void main(String[] args) throws InterruptedException {
		
		WebDriver d4 = new ChromeDriver();
		/*
		d4.get("https://codenboxautomationlab.com/practice/");
		d4.manage().window().maximize();
		WebElement dropDown = d4.findElement(By.xpath("//select[@id = 'dropdown-class-example']"));
		Select s1 = new Select(dropDown);
		// s1.selectByValue("option1");
		//s1.selectByIndex(3);
		s1.selectByVisibleText("Selenium");
		//d4.close();   */
		
		
	} 

}
